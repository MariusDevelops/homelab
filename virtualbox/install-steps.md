```
Created VM in VirtualBox 7.0.16 (Ubuntu build r162802)

Attached Ubuntu Server ISO (ubuntu-26.04-live-server-amd64.iso)

Installed OS with default settings

Enabled OpenSSH Server `ssh user@vm-ip`
```