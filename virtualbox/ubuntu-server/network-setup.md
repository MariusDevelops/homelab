# Network Setup

## Mode
- VirtualBox network mode: NAT

## Description
NAT allows the VM to use the host machine’s internet connection.  
The VM is not directly accessible from other devices on the local network.

## SSH Access
SSH is used to access the VM from the host machine.

Example:
```bash
ssh user@vm-ip